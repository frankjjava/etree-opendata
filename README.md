# etree-referencedata

Important:

Init.
- Extract the "etree-home.zip" file to any location
- On the host set up the ETREE_HOME environment variable to point to the "etree-home" folder.
- Set up DB
	- db-init.sql (loging out and back for different commands is required. Pls go-through the scripts)
	- create-schema.sql
	- run all other "insertXXX.sql" scripts.
	